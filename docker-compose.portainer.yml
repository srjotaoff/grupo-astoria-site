services:
  admin:
    build:
      context: ${PROJECT_PATH:-/opt/apps/astoria}
      dockerfile: Dockerfile
    container_name: astoria-admin
    working_dir: /workspace
    command: npm run dev:admin
    env_file:
      - ${PROJECT_PATH:-/opt//apps/astoria}/.env
    environment:
      NODE_ENV: development
      APP_NAME: admin
      PORT_ADMIN: 3001
      CHOKIDAR_USEPOLLING: "true"
      WATCHPACK_POLLING: "true"
    volumes:
      - ${PROJECT_PATH:-/opt/apps/astoria}:/workspace
      - admin_node_modules:/workspace/node_modules
    expose:
      - "3001"
    restart: unless-stopped
    networks:
      - astoria-net

  chocosul:
    build:
      context: ${PROJECT_PATH:-/opt/apps/astoria}
      dockerfile: Dockerfile
    container_name: astoria-chocosul
    working_dir: /workspace
    command: npm run dev:chocosul
    env_file:
      - ${PROJECT_PATH:-/opt/apps/astoria}/.env
    environment:
      NODE_ENV: development
      APP_NAME: chocosul
      PORT_CHOCOSUL: 3002
      CHOKIDAR_USEPOLLING: "true"
      WATCHPACK_POLLING: "true"
    volumes:
      - ${PROJECT_PATH:-/opt/apps/astoria}:/workspace
      - chocosul_node_modules:/workspace/node_modules
    expose:
      - "3002"
    restart: unless-stopped
    networks:
      - astoria-net

  mastter:
    build:
      context: ${PROJECT_PATH:-/opt/apps/astoria}
      dockerfile: Dockerfile
    container_name: astoria-mastter
    working_dir: /workspace
    command: npm run dev:mastter
    env_file:
      - ${PROJECT_PATH:-/opt/apps/astoria}/.env
    environment:
      NODE_ENV: development
      APP_NAME: mastter
      PORT_MASTTER: 3003
      CHOKIDAR_USEPOLLING: "true"
      WATCHPACK_POLLING: "true"
    volumes:
      - ${PROJECT_PATH:-/opt/apps/astoria}:/workspace
      - mastter_node_modules:/workspace/node_modules
    expose:
      - "3003"
    restart: unless-stopped
    networks:
      - astoria-net

  admin-mastter:
    build:
      context: ${PROJECT_PATH:-/opt/apps/astoria}
      dockerfile: Dockerfile
    container_name: astoria-admin-mastter
    working_dir: /workspace
    command: npm run dev:admin-mastter
    env_file:
      - ${PROJECT_PATH:-/opt/apps/astoria}/.env
    environment:
      NODE_ENV: development
      APP_NAME: admin-mastter
      PORT_ADMIN_MASTTER: 3004
      CHOKIDAR_USEPOLLING: "true"
      WATCHPACK_POLLING: "true"
    volumes:
      - ${PROJECT_PATH:-/opt/apps/astoria}:/workspace
      - admin_mastter_node_modules:/workspace/node_modules
    expose:
      - "3004"
    restart: unless-stopped
    networks:
      - astoria-net

  astoria:
    build:
      context: ${PROJECT_PATH:-/opt/apps/astoria}
      dockerfile: Dockerfile
    container_name: astoria-astoria
    working_dir: /workspace
    command: npm run dev:astoria
    env_file:
      - ${PROJECT_PATH:-/opt/apps/astoria}/.env
    environment:
      NODE_ENV: development
      APP_NAME: astoria
      PORT_ASTORIA: 3005
      CHOKIDAR_USEPOLLING: "true"
      WATCHPACK_POLLING: "true"
    volumes:
      - ${PROJECT_PATH:-/opt/apps/astoria}:/workspace
      - astoria_node_modules:/workspace/node_modules
    expose:
      - "3005"
    restart: unless-stopped
    networks:
      - astoria-net

  admin-astoria:
    build:
      context: ${PROJECT_PATH:-/opt/apps/astoria}
      dockerfile: Dockerfile
    container_name: astoria-admin-astoria
    working_dir: /workspace
    command: npm run dev:admin-astoria
    env_file:
      - ${PROJECT_PATH:-/opt/apps/astoria}/.env
    environment:
      NODE_ENV: development
      APP_NAME: admin-astoria
      PORT_ADMIN_ASTORIA: 3006
      CHOKIDAR_USEPOLLING: "true"
      WATCHPACK_POLLING: "true"
    volumes:
      - ${PROJECT_PATH:-/opt/apps/astoria}:/workspace
      - admin_astoria_node_modules:/workspace/node_modules
    expose:
      - "3006"
    restart: unless-stopped
    networks:
      - astoria-net

  cloudbeaver:
    image: dbeaver/cloudbeaver:latest
    container_name: astoria-cloudbeaver
    expose:
      - "8978"
    volumes:
      - cloudbeaver_data:/opt/cloudbeaver/workspace
    restart: unless-stopped
    networks:
      - astoria-net

  nginx:
    image: nginx:1.27-alpine
    container_name: astoria-nginx
    depends_on:
      - admin
      - chocosul
      - mastter
      - admin-mastter
      - astoria
      - admin-astoria
      - cloudbeaver
    ports:
      - "80:80"
      - "443:443"
      - "3009:3009"
    volumes:
      - ${PROJECT_PATH:-/opt/apps/astoria}/infra/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - ${PROJECT_PATH:-/opt/apps/astoria}/apps/Chocosul/stylesheets:/opt/apps/astoria/apps/Chocosul/stylesheets:ro
      - ${PROJECT_PATH:-/opt/apps/astoria}/apps/Chocosul/images:/opt/apps/astoria/apps/Chocosul/images:ro
      - ${PROJECT_PATH:-/opt/apps/astoria}/apps/Mastter/stylesheets:/opt/apps/astoria/apps/Mastter/stylesheets:ro
      - ${PROJECT_PATH:-/opt/apps/astoria}/apps/Mastter/images:/opt/apps/astoria/apps/Mastter/images:ro
      - ${PROJECT_PATH:-/opt/apps/astoria}/apps/Astoria/stylesheets:/opt/apps/astoria/apps/Astoria/stylesheets:ro
      - ${PROJECT_PATH:-/opt/apps/astoria}/apps/Astoria/images:/opt/apps/astoria/apps/Astoria/images:ro
    restart: unless-stopped
    networks:
      - astoria-net

networks:
  astoria-net:
    driver: bridge

volumes:
  admin_node_modules:
  chocosul_node_modules:
  mastter_node_modules:
  admin_mastter_node_modules:
  astoria_node_modules:
  admin_astoria_node_modules:
  cloudbeaver_data:

